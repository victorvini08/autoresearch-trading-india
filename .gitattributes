# Data stores (storage/*.duckdb, *.sqlite) are NOT tracked on this branch —
# they are gitignored and regenerated locally (see README "Getting the data").
# No Git LFS entries: the library ships code only, never bundled market data.
